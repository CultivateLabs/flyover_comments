<% if @comment.destroyed? || params[:hide_after_deletion].present? %>
$("[id=<%= params[:hide_after_deletion] %>").remove();
<% else %>
$("[id=flyover_comment_<%= @comment.id %>]").replaceWith('<%= escape_javascript(render @comment) %>');
<% end %>
