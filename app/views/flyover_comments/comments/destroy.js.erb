<%= if @comment.destroyed? %>
$("[id=flyover_comment_<%= @comment.id %>]").remove();
<% else %>
$("[id=flyover-comment-<%= @comment.id %>]").replaceWith('<%= escape_javascript(render @comment) %>');
<% end %>
